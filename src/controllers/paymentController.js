const Payment = require('../models/Payment');
const { Op } = require('sequelize');

const parkingPayment = async (req, res) => {
    try {
        
    } catch (error) {
        res.status(400).json({ message: error.message });
    }
}
